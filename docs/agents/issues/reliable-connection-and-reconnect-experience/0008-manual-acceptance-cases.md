# 二期可靠连接与重连 — 手工验收用例

> Status: active · 适用构建：含 per-job 取消修复的 debug 构建（`versionName 0.1.0`，`lastUpdateTime ≥ 2026-07-11 11:38`）。
> 用途：在真机（HyperOS 3.0 / Android 16）上由人工逐项执行，记录实际结果与系统前置条件。
> 规格/契约依据：`0006-reliable-reconnect-spec.md`、`0007-integration-contract.md`、`AGENT_HANDOFF.md` §验收矩阵。

---

## 0. 执行前准备

1. 设备已连 wifi，屏幕解锁，App 已装且 profile 已绑定并**处于「接收中」基线**（首页显示「接收中」，诊断页 SSE=已连接、有最近心跳）。
2. 确认基线在线：执行【命令 P1】发一条探针，应返回 `HTTP 201` 且手机收到通知。
3. （可选）`adb` 已连：`adb devices -l` 能看到设备。本表操作以**设备 UI 为主**，adb 仅为辅助观测。
4. 每条用例执行前，先把设备恢复到「接收中」基线，避免相互干扰（用例内另有说明除外）。

## 1. 命令速查（复制即用）

> 所有服务端命令在 VPS 执行（`ssh -p5022 mk@cloud.makia98.com '...'`），token 只进 Authorization 头、**从不回显**，仅打印 HTTP 状态码/响应体/日志。远程 `/home/mk/docker-compose/notice` 与 `docker logs notice` 是尚未迁移的历史运维名，不是产品名。

- **P1 发探针通知**（返回 `201`=在线实时投递 / `202`=离线排队 / `429`=积压满 / `409`=幂等冲突 / `401,403`=鉴权失败 / `404`=接收端不存在）：

  ```sh
  ssh -p5022 mk@cloud.makia98.com 'set -a; . /home/mk/docker-compose/notice/.env; set +a; \
  echo "HTTP $(curl -s -o /tmp/p.body -w "%{http_code}" -X POST http://127.0.0.1:8788/api/v1/notifications \
    -H "Authorization: Bearer $BOOTSTRAP_WEBHOOK_ACCESS_TOKEN" -H "Content-Type: application/json" \
    -d "{\"receiver_id\":\"$BOOTSTRAP_RECEIVER_ID\",\"title\":\"测试\",\"body\":\"手验\",\"priority\":\"normal\"}")"; \
  echo "body: $(cat /tmp/p.body)"; rm -f /tmp/p.body'
  ```
  > 发**离线积压**时把 `priority` 改 `urgent`、`title` 各不相同（如 `离线1/离线2/离线3`），便于核对顺序与去重。

- **S1 看服务端 SSE 连接**（每次连接关闭才打一行 `dur=`；持有中的长连不出现 = 稳定在线）：

  ```sh
  ssh -p5022 mk@cloud.makia98.com 'docker logs notice --since 3m 2>&1 | grep GET | grep /stream | \
  sed -E "s/.*\"time\":\"([^\"]+)\".*duration_ms\":([0-9]+).*/\1  dur=\2ms/" | sort'
  ```
  > 出现连续多条 `dur` 为 0–1200ms 的行 = 重连抖动/风暴；只有 0–1 条短连随后安静 = 收敛稳定。

- **S2 看 webhook 投递结果**（`status` 200=在线已投/202=排队/429=满）：

  ```sh
  ssh -p5022 mk@cloud.makia98.com 'docker logs notice --since 3m 2>&1 | grep POST | grep notifications | \
  sed -E "s/.*\"time\":\"([^\"]+).*\"status\":([0-9]+).*/\1 HTTP \2/" | sort'
  ```

- **D1 打开诊断页**（免手动翻菜单）：`adb shell am start -n com.makia98.electricwave/.MainActivity --es nav diagnostics`
- **D2 看前台服务是否存活**：`adb shell dumpsys activity services com.makia98.electricwave | grep -i isForeground`（有输出=存活）
- **D3 看 App 进程**：`adb shell pidof com.makia98.electricwave`（空=已被杀）
- **D4 杀进程（非强停，模拟系统回收）**：`adb shell am kill com.makia98.electricwave`
- **D5 强行停止**：`adb shell am force-stop com.makia98.electricwave`（或 设置→应用→强行停止）
- **D6 切网络（需 root 或开发者权限；多数真机用系统设置/控制中心操作）**：
  `adb shell svc wifi disable` / `adb shell svc wifi enable`（失败则改用控制中心）

## 2. 状态与术语（预期结果以此为准）

- **6 个首页状态**（`UiStatus`）：`接收中` / `正在重连` / `有待补发` / `需要授权` / `已暂停` / `不可用`。
- **诊断页关键字段**：SSE 状态、最近心跳、最近连接、最近断开、下次重连时间、积压数量、**最老积压时间**、最近 ack event id、通知权限、默认/紧急/前台渠道、错误分类与下一步动作。
- **连接后台态**（`ConnectionStatus`）：`CONNECTED`→接收中；`CONNECTING/BACKOFF`→正在重连；`AUTH_FAILED/NOT_FOUND/CONFIG_ERROR`→不可用（停止重试）。
- **服务启动点（仅这几处会真正建连）**：① 首页开关切到「开」；② 「立即重连/重新绑定」主按钮；③ 开机自启（`BootReceiver`，受 HyperOS 自启动限制）；④ debug 指令。
  > ⚠️ **冷启动 App 本身不会自动重连**（`Application.onCreate` 不启动服务）——进程被杀后，必须重新切开关或点主按钮才会重连。这是实现现状，相关用例请照实记录。

---

## 3. 测试用例

> 列：**ID | 场景 | 系统前置条件 | 操作步骤 | 预期结果 | 通过 | 备注**。
> 「通过」列执行时填 ✅/❌；「备注」记录实际观测与前置条件细节。

### A. 基本启停与一键重连

| ID | 场景 | 系统前置条件 | 操作步骤 | 预期结果 | 通过 | 备注 |
|----|------|------------|---------|---------|------|------|
| A1 | 首次开启接收 | profile 已绑定、开关为关 | ① 首页把接收开关切到「开」 | 状态：连接中→**接收中**；【S1】出现 1 条 stream 长连并持有（无抖动）；【P1】返回 201 | ☐ |  |
| A2 | 停止接收 | 处于「接收中」 | ① 首页开关切到「关」 | 状态：**已暂停**；【S1】长连关闭；【D2】前台服务停止 | ☐ |  |
| A3 | 一键重连（正在重连态） | 处于「正在重连」（可先断网再恢复制造） | ① 点首页主按钮「立即重连」 | 退避被重置，立即重连；状态→**接收中**；【S1】仅 1 条干净重连 | ☐ |  |
| A4 | 主按钮（不可用态恢复） | 处于「不可用」（见 G 系列） | ① 修正配置后点「重新绑定/检查配置」 | 状态→**接收中**；【P1】返回 201 | ☐ | 配置未修则仍「不可用」 |

### B. 生命周期重连

| ID | 场景 | 系统前置条件 | 操作步骤 | 预期结果 | 通过 | 备注 |
|----|------|------------|---------|---------|------|------|
| B1 | App 冷启动 | profile 开、处于「接收中」 | ①【D4】杀进程 ② 重新打开 App | ⚠️ **实现不自动重连**：首页可能显示旧状态但无实际连接；需切开关或点「立即重连」才恢复 → **接收中** | ☐ | 记录：仅打开 App 是否已重连（预期否） |
| B2 | 回前台触发重连 | 服务存活、处于「接收中」 | ① 按 Home 进后台 ② 等 ≥10s ③ 切回 App | 若连接健康→跳过（日志 `Reconnect skipped: connection healthy`）；若已掉线→立即重连→**接收中** | ☐ | 触发需服务在运行 |
| B3 | 进后台 | 处于「接收中」 | ① 按 Home；【D2】确认前台服务仍在 | 前台服务**保持存活**（`isForeground=true`）；连接不断；【P1】仍 201 | ☐ |  |
| B4 | 前台服务重启 | 处于「接收中」 | ①【D5】强停后用 ④debug 指令或开关重启服务 | 服务重建 → 重连 → **接收中**；【S1】收敛后稳定持有 | ☐ | 等同 A1 重建路径 |
| B5 | 设备重启 | HyperOS 自启动=**开** | ① 重启手机 ② 等开机完成 | `BootReceiver` 尝试自启 → **接收中**；【P1】201 | ☐ | 自启动关则不恢复（见 F5） |

### C. 网络切换与飞行模式

| ID | 场景 | 系统前置条件 | 操作步骤 | 预期结果 | 通过 | 备注 |
|----|------|------------|---------|---------|------|------|
| C1 | 断网→退避→恢复 | 处于「接收中」 | ① 关 wifi/数据 ② 等 ~10s ③ 观察首页 ④ 开 wifi/数据 | 断网后→**正在重连**（退避 1/2/5…s）；恢复后→`onAvailable` 立即重连→**接收中**；【S1】恢复后仅 1 条干净重连 | ☐ |  |
| C2 | 飞行模式开/关 | 处于「接收中」 | ① 开飞行模式 ② 等 ~10s ③ 关飞行模式 | 同 C1：开→正在重连；关→**接收中** | ☐ |  |
| C3 | 切换网络 | 处于「接收中」 | ① wifi 切到移动数据（或反之） | 短暂「正在重连」后→**接收中**；【P1】201 | ☐ | 记录是否出现重复通知（应无） |

### D. SSE 可靠性（积压/补发/去重/退避）

| ID | 场景 | 系统前置条件 | 操作步骤 | 预期结果 | 通过 | 备注 |
|----|------|------------|---------|---------|------|------|
| D1 | 离线积压+补发 | 处于「接收中」 | ① 关网络 ② 连发 3 条【P1】（title=离线1/2/3，应 `202`） ③ 开网络 | 手机按顺序收到 3 条、**无重复**；诊断页「最近 ack event id」递增、积压归 0；【S2】先 202 后恢复在线 | ☐ | 核对顺序与条数 |
| D2 | 重连不重复投递 | 接上一条已 ack | ①【D4】杀进程 ② 重启服务重连 | 已 ack 的事件**不再补发**；无重复通知 | ☐ | 依赖 event_id 游标去重 |
| D3 | backlog_gap（可选/高级） | 存在已过期被丢的事件 | ① 让部分积压超过 TTL（>60s 起算，长 TTL 需等） ② 重连 | 诊断页可能显示错误分类=`backlog_gap`、`有待补发`；【P1】补缺口后段 | ☐ | 难手动触发，记录是否出现 |
| D4 | Retry-After / 429（高级） | 服务端返回 429 | ① 灌满积压（>1000 条）或触发限流 | 状态→正在重连（尊重 Retry-After，≤300s）；恢复后→**接收中** | ☐ | 需大量请求，可选 |
| D5 | 心跳超时重连 | 处于「接收中」 | ① 制造网络黑洞（连 wifi 但断外网，如关路由器上行） ② 等 ~75s | ~75s 无心跳→判死链→**正在重连**→退避 | ☐ | 较难干净制造，可选 |
| D6 | 退避节奏 | 处于「正在重连」 | ① 持续断网观察诊断页「下次重连时间」 | 间隔呈 1/2/5/10/30/60s… 上限 300s，带抖动 | ☐ |  |

### E. 通知权限与渠道

| ID | 场景 | 系统前置条件 | 操作步骤 | 预期结果 | 通过 | 备注 |
|----|------|------------|---------|---------|------|------|
| E1 | 关闭 App 通知权限 | 处于「接收中」 | ① 系统设置→应用→通知→关「允许通知」 ② 回 App 看 | 首页→**需要授权**；开启后刷新诊断→**接收中** | ☐ |  |
| E2 | 关闭默认渠道 | 处于「接收中」 | ① 通知设置里把「默认」渠道设为无 | 诊断页默认渠道=关；首页→**需要授权**；恢复后→接收中 | ☐ |  |
| E3 | 关闭紧急渠道 | 处于「接收中」 | ① 把「紧急」渠道设为无 | 同 E2：→**需要授权** | ☐ |  |
| E4 | 前台渠道可见性 | 处于「接收中」 | ① 确认状态栏有前台服务常驻通知 ② 关「前台」渠道 | 前台通知不可见时→**需要授权**（提示影响后台运行） | ☐ | 关前台渠道可能影响 FGS，谨慎 |
| E5 | POST_NOTIFICATIONS 权限 | Android 13+ | ① 撤销运行时通知权限 ② 重开 | →**需要授权**；授予后→接收中 | ☐ |  |

### F. 系统策略边界（可能无法自动恢复，**逐项记录前置条件**）

> 这些项 App **不承诺绕过**系统限制。记录：触发后是否仍能收到、延迟多久、需要什么手动动作恢复。

| ID | 场景 | 系统前置条件 | 操作步骤 | 预期/记录 | 通过 | 备注 |
|----|------|------------|---------|----------|------|------|
| F1 | Doze 5 分钟 | 熄屏、未充电 | ① 熄屏 5min，中途【P1】发 1 条 ② 亮屏观察 | 记录：是否延迟到亮屏/维护窗才收到、延迟多久 | ☐ | 记充电状态、是否准点 |
| F2 | Doze 30 分钟 | 同上 | ① 熄屏 30min，中途发 2 条 | 同 F1 记录 | ☐ |  |
| F3 | Doze 120 分钟 | 同上 | ① 熄屏 120min，中途发 2 条 | 同 F1 记录 | ☐ | 长时项，可后台跑 |
| F4 | 最近任务清理 | 处于「接收中」 | ① 最近任务里划掉 App ②【D2】查服务 ③【P1】 | 记录：前台服务是否存活、是否仍 201（HyperOS 可能杀） | ☐ | 关键看 FGS 存活 |
| F5 | 强行停止 | 处于「接收中」 | ①【D5】强停 ② 发【P1】 ③ 不手动开 App 等待 | **不自动恢复**（边界）；记录：是否完全不收。手动重开+切开关后恢复 | ☐ | 设计即如此 |
| F6 | Android 13+ FGS Stop | 处于「接收中」 | ① 任务管理器/通知里「停止」前台服务 ②【P1】 | **不自动恢复**；手动恢复后→接收中 | ☐ |  |
| F7 | HyperOS 自启动=关 | 自启动设为关 | ① 重启手机 ② 等 2min ③【P1】 | 记录：`BootReceiver` 是否被拦、是否不收；开自启动后恢复 | ☐ |  |
| F8 | HyperOS 电池=受限 | 无限制→受限 | ① 设为「受限」 ② 熄屏 30min 发 1 条 | 记录：受限下 Doze 行为差异、是否延迟/丢失 | ☐ | 与 F2 对比 |

### G. 认证与配置级阻断（→不可用，停止重试）

| ID | 场景 | 系统前置条件 | 操作步骤 | 预期结果 | 通过 | 备注 |
|----|------|------------|---------|---------|------|------|
| G1 | 鉴权失败 401/403 | 处于「接收中」 | ① 在配置页把 identity token 改错 ② 触发重连（开关或按钮） | 状态→**不可用**（鉴权失败），**停止重试**；服务端日志 401/403 | ☐ | 改回正确后→A4 恢复 |
| G2 | 接收端不存在 404 | 处于「接收中」 | ① 把 receiver_id 改错 ② 触发重连 | →**不可用**（接收端不存在）；服务端 404 | ☐ |  |
| G3 | endpoint/TLS 配置错误 | 处于「接收中」 | ① 把 endpoint 改成无效/证书错误地址 ② 触发重连 | →**不可用**（配置错误，含 TLS）；**不退避** | ☐ |  |
| G4 | 配置修复后恢复 | 处于 G1/G2/G3 | ① 改回正确配置 ② 点「重新绑定」 | →**接收中**；【P1】201 | ☐ |  |
| G5 | 日志脱敏核验 | 任意 | ①【S1】【S2】翻看服务端日志 | 日志中**无** Authorization/token 明文、**无**完整通知正文（仅截断/字段） | ☐ | 安全项 |

---

## 4. 结果记录模板（执行时复制填写）

```text
执行人：        设备/系统：HyperOS 3.0 / Android 16   构建 lastUpdateTime：
执行日期：      基线 receiver：phone-main（非凭证）

| 用例ID | 执行时间 | 系统前置条件（实测） | 实际结果（UI/服务端/设备） | 通过 | 备注/偏差 |
|--------|---------|--------------------|--------------------------|------|----------|
| A1     |         |                    |                          | ☐    |          |
| ...    |         |                    |                          |      |          |
```

## 5. 已知边界声明（不视为缺陷）

- 强行停止、Android 13+ FGS Stop、HyperOS 自启动/电池受限、长时间 Doze 下，App **不承诺**自动恢复；需用户手动恢复或调整系统设置（见 F5–F8）。
- 冷启动 App 本身不自动重连（需切开关或点主按钮），见 B1。
- 顶部弹出、锁屏显示、勿扰下投递由系统/渠道/用户设置决定，App 无法强制保证。
- ack 仅表示「App 已收到并持久化」，**不**等于「系统通知已展示」。
